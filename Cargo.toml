[package]
name = "tailflag"
version = "0.1.0"
edition = "2021"

[dependencies]
ksni = { version = "0.3", default-features = false, features = ["blocking", "async-io"] }
serde_json = "1"
png = "0.17"

[profile.release]
strip = true
lto = true
